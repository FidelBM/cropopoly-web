/*
Autor:            Fidel Bonilla

El código son los datos de conexion al juego
*/

// Puerto en el que se ejecutará el servidor, obtenido de la variable de entorno LINK o por defecto el puerto 8000
export const LINK = process.env.LINK || "https://localhost:8000";
